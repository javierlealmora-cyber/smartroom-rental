# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (IMPORTANT: Never commit credentials)
.env
.env.local
.env.development
.env.staging
.env.production

# Supabase
supabase/.temp
supabase/.branches
supabase/functions/**/node_modules
supabase/backups

# Local Claude settings
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
# .vscode/settings.json intentionally excluded — may contain secrets (service role keys, etc.)
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*.local

# E2E auth state y credenciales
tests/e2e/.auth/
tests/e2e/.env.e2e

# Local DB debugging dumps / introspección ad-hoc
dump-help.txt
schema-dev.sql
schema-pull.sql
query-locks.sql
locks-ddl.json

# Playwright MCP session artifacts (capturas/snapshots exploratorios)
.playwright-mcp/

# Codex/agent scratch output (no forma parte del producto)
.codex-output/

# MCP config local (específica de cada máquina, puede variar por dev)
.devin/mcp_config.json
.devin/config.json
